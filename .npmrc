# @deepseek-ai/* peer packages are provided at dev time by scripts/link-dsh.mjs
# (symlinks into the deepseek-harness checkout) and at install time by the
# host harness itself. Never auto-install them from the registry here.
legacy-peer-deps=true
